import React from 'react';
import { View, Text } from "react-native";

export default RegisterCap = () => (
			<View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
				<Text>Cadastrar Cap</Text>
			</View>
		);